export function AuthCallbackPage() {
  return (
    <main className="login-page">
      <section className="login-card login-card--compact" aria-label="Signing in">
        <div className="login-card__body">
          <h1 className="login-card__title">Signing You In</h1>
          <p className="login-form__hint">Please wait while we complete your secure login.</p>
        </div>
      </section>
    </main>
  )
}
